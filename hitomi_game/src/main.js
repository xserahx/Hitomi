const config = {
  canvas_width: 800,
  canvas_height: 600,
  canvas_id: 'game',
  background_color: 0x87ceeb,
  debug_mode: true,
  gravity_value: 600
}

// Create the game with PoliPhazer
PP.game.create(config)
